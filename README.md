# Swift Code

Swift rewrite of Claude Code. The TypeScript reference implementation is preserved under `.reference/`.

## Build

```bash
swift build
```

## Run

```bash
swift run swiftcode -- --version
```
